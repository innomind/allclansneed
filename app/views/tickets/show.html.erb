Betreff: <%= @ticket.subject %><br />

<%= render :partial => "message", :collection => @messages %>

Antwort:
<% error_handling_form_for @answer, :url => ticket_messages_path do |form| %>
      <%= form.hidden_field :ticket_id, :value => @ticket.id %>
      <%= form.text_area :message, :rows => 5, :cols => 30%>
      <%= form.submit "erstellen" %>
<% end %>


<%= link_to 'Back', tickets_path %>