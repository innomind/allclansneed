<% error_handling_form_for @forum_thread, :url => forum_forum_threads_path(params[:forum_id]) do |f| %>
    <%= f.text_field 'title' %>
    
    <% fields_for "forum_thread[forum_message]" do |message_form| %>
      <%= message_form.text_area 'message', :rows => 10, :cols => 40 %>
    <% end %>
      <%= f.submit 'eintragen' %>
<% end %>

