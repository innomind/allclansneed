<h1>Gruppe <%= @group.name %></h1>

<h3>Gruppe bearbeiten:</h3>
  <%= link_to image_tag("page_edit.png", :border => 0), :action => 'edit', :id => @group.id %>

<h3>pending members</h3>
  <%= render :partial => 'user_pending', :collection => @pending_memberships %>

<h3>active members</h3>
  <%= render :partial => 'user_active', :collection => @active_memberships %>