<% error_handling_form_for @page do |form| %>
	<%= form.text_field :title %>
	<%= form.text_area :content, :rows => 5, :cols => 30 %>
	<%= form.check_box :intern %>
	<%= form.submit "Seite bearbeiten"%>
<% end %>