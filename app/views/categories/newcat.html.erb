<% error_handling_form_for @category, :url => categories_path(:id => @cat_name) do |f| %>
	<%= f.hidden_field :section, :value => params[:section], :style => "blank" %>
	<%= f.text_field :name %>
	<%= f.submit "erstellen" %>
<% end %>