<% if @classifieds.blank? %>
	<p>keine Einträge<p>
<% else %>

	
	<%= text_field "classified", "title" -%>
	
	<%= observe_field :classified_title,
		:frequency => 1.0,
		:update => 'classifieds',
		:url => {:controller => 'classified', :action => 'search' },
		:with => "'search=' + encodeURIComponent(value)" %>
	
	<ul id="classifieds">
		<%= render :partial => "classified", :collection => @classifieds %>
	</ul>
<% end %>

<%= link_to "Add new ad", :action => "new" %>