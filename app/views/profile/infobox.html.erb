<%= image_tag(@profile.profile_pic.url(:thumb)) %>
<div class="infobox_container">
	<ul class="link_list">
		<li><%= link_to image_tag("user.png") + " Profil", profile_path(@profile.user.id)%></li>
		<li><%= access_link image_tag("email.png") + " Nachricht schreiben", new_message_path(:receiver_id => @profile.user.id)%></li>
	</ul>
	<h4>Verbindung:</h4>
	<%= show_connection %><br>
	<%= become_friends_link @profile.user %>
</div>