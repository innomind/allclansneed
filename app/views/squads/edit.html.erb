<h1>Editing squad</h1>

<% form_for(@squad) do |f| %>
  <%= f.error_messages %>

  <p>
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </p>
  <p>
    <%= f.submit "Update" %>
  </p>
<% end %>

<%= link_to 'Show', @squad %> |
<%= link_to 'Back', squads_path %>
