<% error_handling_form_for @poll, :url => vote_poll_path(@poll), :html => {:method => :post} do |form| %>
  <h1>Voten für:<%= @poll.title %></h1>
  Beschreibung: <%= @poll.description %><br />
    <%= render :partial => "poll_option", :collection => @poll.poll_options %>
  <%= form.submit "voten" %>
<% end %>