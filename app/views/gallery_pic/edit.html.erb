<% error_handling_form_for :gallery_pic, @gallery_pic, :url => pic_path(@gallery_pic), :html => { :multipart => true, :method => :put } do |form| %>
  <%= form.file_field :pic %>
  <%= form.text_field :name %>
  <%= form.text_area :description, :rows => "5", :cols => "15" %>
  <%= form.submit "abschicken" %>
<% end %>